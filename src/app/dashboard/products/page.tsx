import TableDataProducts from '@/features/products/componets/TableDataProducts';

export const metadata = {
  title: 'Dashboard : Kanban view'
};

export default function page() {
  return <TableDataProducts />;
}
