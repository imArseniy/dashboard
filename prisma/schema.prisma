generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model OrderStats {
  id       Int      @id @default(autoincrement())
  date     DateTime
  order    Int
  delivery Int
}